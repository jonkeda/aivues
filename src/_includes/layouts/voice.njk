<!DOCTYPE html>
<html lang="{{ lang }}">
<head>
  <meta charset="utf-8">
  <title>{{ title }}</title>
  <meta name="robots" content="noindex">
  <meta name="description" content="{{ description }}">
</head>
<body>
{{ content | safe }}
</body>
</html>
