{
  "name": "aivues",
  "version": "0.1.0",
  "description": "Smart glasses guide for visually impaired people",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --port=3000",
    "build": "cross-env NODE_ENV=production npx @11ty/eleventy && npx pagefind --site _site --glob \"**/*.html\""
  },
  "keywords": [
    "accessibility",
    "visual-impairment",
    "smart-glasses",
    "ray-ban-meta"
  ],
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "clean-css": "^5.3.3",
    "cross-env": "^10.1.0",
    "markdown-it-attrs": "^4.3.1",
    "pagefind": "^1.5.0",
    "terser": "^5.46.1"
  }
}
